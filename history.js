import createHistory from 'history/createBrowserHistory';
// Was inititially used in place of browser history, but found that browserhistory is a better alternative to keep track of route
export default createHistory();